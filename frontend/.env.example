# URL of the FastAPI engine (server-side proxy target)
ENGINE_API_URL=http://localhost:8000
# API key forwarded to the engine (matches src/api/middleware/api_key.py)
ENGINE_API_KEY=dev-key
# Optional HTTP basic auth for pilot deployments
# CONSOLE_AUTH_USER=clinician
# CONSOLE_AUTH_PASSWORD=change-me
